/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard Vercel/Next deployment (recommended)
  // Remove GitHub Pages static export and basePath behavior.
};

export default nextConfig;